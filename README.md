# Backend Test!

### Uses Express, Multer, Mongoose and Nodemailer along with custom built wrappers and hooks to provide a lots of scalable features!

Run `npm install` and `npm start` to run the project!
